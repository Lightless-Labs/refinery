# Optional direct Codex CLI API keys (pay-per-use)
OPENAI_API_KEY=your-api-key-here

# Alternative credential for codex exec (uncomment to use)
# CODEX_API_KEY=sk-proj-...

# Pi and OpenCode manage credentials in their own local config:
#   pi --list-models
#   opencode auth
